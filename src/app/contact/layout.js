export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with Workitu Tech. We typically respond within 24 hours.',
};

export default function ContactLayout({ children }) {
  return children;
}
